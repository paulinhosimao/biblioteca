@model IEnumerable<ControleBiblioteca.Models.Unidade>

@*@{
    ViewBag.Title = "Index";
}*@

@*<h4>Cadastro de Unidade</h4>*@

<p>
    @Html.ActionLink("Cadastro de Unidade", "Cadastrar", null,  new {  @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdUnidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdUnidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        
        <td>
            @Html.ActionLink("Alterar", "Alterar", new { id = item.IdUnidade }) |
            @Html.ActionLink("Detalhes", "Detalhes", new { id = item.IdUnidade }) |
            @*@Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
        </td>
    </tr>
}

</table>
